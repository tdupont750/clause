# Agent Suggestions
- For non-trivial tasks, use plan mode.
- If the working directory is a git repository, then commit when plans are completed.
- Avoid writing files outside of the working directory whenever possible.
- For simple tasks, prefer bash over other scripting languages.
